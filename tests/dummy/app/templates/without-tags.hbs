<h3 id="title">Select multiple without tags enabled</h3>
<div>
    {{#select2-select
        action="onChangeSelect"
        config=config
        items=items
        selectId="selectWithoutTags"}}

        <select id="selectWithoutTags" multiple>
            {{#each items as |item|}}
                <option value="{{item.id}}" selected={{item.selected}} data-label="{{item.label}}">
                    {{item.label}} ({{item.count}})
                </option>
            {{/each}}
        </select>

    {{/select2-select}}
</div>

<h3>log ({{log.length}})</h3>
<div id="log">
    <ul>
    {{#each log as |item|}}
        <li>{{item}}</li>
    {{/each}}
    </ul>
</div>
{{outlet}}
